const BACKUP_TRACKER: str = "tracker.openbittorrent.com:80";

const MAX_RETRIES: u32 = 8;
const INITIAL_TIMEOUT: u32 = 100000000; // in nanoseconds // set to 100 ms
const IP_ADDR: &str = "0.0.0.0";
const PORT: u16 = 6881;
